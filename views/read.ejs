<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
    rel="stylesheet"
/>
    <title>Read</title>
</head>
<body>
    <div id="main" class="bg-zinc-900 h-screen w-full p-10 flex flex-col gap-7">
        <a href="/" class="text-blue-500">Create Profie <i class="ri-arrow-right-s-line"></i></a>
        <h1 class="text-2xl text-white">All Users</h1>
        <div id="box" class="w-full h-full flex bg-zinc-900 relative flex-wrap gap-5">
            <% users.forEach((e)=>{ %>
                <div id="card" class="h-80 w-72 rounded-xl bg-zinc-800 p-5">
                    <div id="image" class="h-48 w-full bg-blue-500 rounded-lg overflow-hidden">
                        <img class="object-cover" src="<%= e.img %>" alt="load nahi hora bhai...">
                    </div>
                    <h1 class="text-white text-2xl mt-4"><%= e.name %></h1>
                    <h3 class="text-slate-500"><%= e.email %></h3>
                    <div id="wrapper" class="flex items-center justify-between">
                        <a class="text-slate-300" href="">Edit</a>
                        <a class="text-red-500" href="/delete/:<%= e._id %>">Delete</a>
                    </div>
                </div>

            <% }) %>
        </div>
    </div>
</body>
</html>